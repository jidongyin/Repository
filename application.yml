spring:
  profiles:
    active:
     - dev
---
spring:
  profiles: dev #开发环境
    application:
        name: springcloud-dept-config-dev

---
spring:
  profiles:
    active:
     - test
---
spring:
  profiles: test #测试环境
    application:
        name: springcloud-dept-config-test
